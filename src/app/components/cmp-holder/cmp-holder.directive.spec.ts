import { CmpHolderDirective } from './cmp-holder.directive';

describe('CmpHolderDirective', () => {
  it('should create an instance', () => {
    const directive = new CmpHolderDirective();
    expect(directive).toBeTruthy();
  });
});
